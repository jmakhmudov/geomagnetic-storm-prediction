interface SolarProps {
    title: string,
    data: string,
    trend: 1 | 0 | -1,
    changed: number
}

const SolarData = ({title, data, trend, changed}: SolarProps) => {

    return (
        <div>
            
        </div>
    );
}

export default SolarData;